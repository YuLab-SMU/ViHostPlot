% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration_signature.R
\name{plot_integration_signature}
\alias{plot_integration_signature}
\title{Plot integration signatures for clonality or breakpoint orientation}
\usage{
plot_integration_signature(
  integrations,
  mode = c("clonality", "strand"),
  group_by = "sample",
  cluster_by = c("host", "host_virus"),
  cluster_window = 1000,
  measure = c("support", "events"),
  top_n = 10,
  colors = NULL
)
}
\arguments{
\item{integrations}{Integration records accepted by \code{\link[=as_integrations]{as_integrations()}}.}

\item{mode}{Signature view to draw. \code{"clonality"} summarizes candidate
recurrent breakpoint clusters; \code{"strand"} summarizes \code{virus_strand}
orientation.}

\item{group_by}{Optional column used for stacked composition.}

\item{cluster_by}{Clustering rule for \code{mode = "clonality"}. \code{"host"} groups
nearby host breakpoints; \code{"host_virus"} additionally requires nearby viral
breakpoints.}

\item{cluster_window}{Distance in base pairs used to group nearby
breakpoints.}

\item{measure}{Quantity to summarize: number of integration events
(\code{"events"}) or total supporting reads (\code{"support"}).}

\item{top_n}{Number of candidate clusters shown in \code{mode = "clonality"}.}

\item{colors}{Optional fill colors. A named vector can be used to set colors
for specific groups.}
}
\value{
A \code{ggplot} object.
}
\description{
Summarize integration records as evidence views for candidate clone-like
breakpoint clusters or viral-strand orientation. The clonality view groups
nearby breakpoints using a simple distance window; it is intended as a
visualization of recurrent evidence, not as an automatic biological
clonality call.
}
