% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interactive_explorer.R
\name{plot_interactive_explorer}
\alias{plot_interactive_explorer}
\title{Create an interactive integration explorer}
\usage{
plot_interactive_explorer(
  integrations,
  host = "hg38",
  virus,
  features = NULL,
  chrom_file = NULL,
  group_by = "method",
  size_by = "support",
  table_columns = NULL,
  colors = NULL,
  point_size = 2.8,
  size_range = c(2, 6),
  width = 10,
  height = 5
)
}
\arguments{
\item{integrations}{Integration records accepted by \code{\link[=as_integrations]{as_integrations()}}.}

\item{host}{Host genome specification. Can be a built-in host name, a host
genome table, or a \code{\link[=host_genome]{host_genome()}} object.}

\item{virus}{Virus genome specification. Can be a \code{\link[=virus_genome]{virus_genome()}} object or
a named numeric vector such as \code{c(HBV = 3215)}.}

\item{features}{Optional virus feature intervals from \code{\link[=virus_features]{virus_features()}}.}

\item{chrom_file}{Optional path to a chromosome-size table. Supply either
\code{host} or \code{chrom_file}, not both.}

\item{group_by}{Optional column in \code{integrations} mapped to point color.}

\item{size_by}{Optional numeric column in \code{integrations} mapped to point size.}

\item{table_columns}{Optional columns to show in the data table. Core
columns are used by default.}

\item{colors}{Optional vector of colors. A named vector can be used to set
colors for specific groups.}

\item{point_size}{Point size used when \code{size_by} is \code{NULL}.}

\item{size_range}{Point-size range used when \code{size_by} is supplied.}

\item{width}{SVG width passed to \code{\link[ggiraph:girafe]{ggiraph::girafe()}}.}

\item{height}{SVG height passed to \code{\link[ggiraph:girafe]{ggiraph::girafe()}}.}
}
\value{
An HTML tag list containing a \code{ggiraph} widget and a \code{DT} table.
}
\description{
Build a lightweight HTML explorer that combines an interactive host-virus
breakpoint map with a searchable integration table. Points use stable
record identifiers as \code{ggiraph} data IDs, and the same identifiers are shown
in the table for cross-reference.
}
