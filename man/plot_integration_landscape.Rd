% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landscape.R
\name{plot_integration_landscape}
\alias{plot_integration_landscape}
\title{Plot the host-genome integration landscape}
\usage{
plot_integration_landscape(
  integrations,
  host = "hg38",
  chrom_file = NULL,
  mode = c("sites", "density"),
  group_by = NULL,
  bin_size = 5e+06,
  colors = NULL,
  point_size = 2.2
)
}
\arguments{
\item{integrations}{Integration records accepted by \code{\link[=as_integrations]{as_integrations()}}.}

\item{host}{Host genome specification. Can be a built-in host name, a host
genome table, or a \code{\link[=host_genome]{host_genome()}} object.}

\item{chrom_file}{Optional path to a chromosome-size table. Supply either
\code{host} or \code{chrom_file}, not both.}

\item{mode}{Display individual integration sites (\code{"sites"}) or counts in
genomic bins (\code{"density"}).}

\item{group_by}{Optional column in \code{integrations} mapped to color.}

\item{bin_size}{Bin width in base pairs for \code{mode = "density"}.}

\item{colors}{Optional vector of colors. A named vector can be used to set
colors for specific groups.}

\item{point_size}{Point size for \code{mode = "sites"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Display viral integration events across a host genome as individual sites
or binned event density. Chromosomes are placed on a continuous genomic axis
in the order supplied by the host genome.
}
